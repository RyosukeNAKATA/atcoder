use std::usize;

use proconio::marker::{Bytes, Chars};
use proconio::{fastout, input};

#[fastout]
fn main() {
    input! {
        n: u64,
        b: u64,
        k: usize,
        c: [u64; k],
    }
}
